import 'package:flutter/material.dart';

class ItensFiltro extends StatelessWidget {
  const ItensFiltro({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}